import React from "react";

const PreFooter = () => {
  return (
    <div className=" uppercase font-ivy text-[min(20vw,20vh)] relative ">
      <p className="relative -bottom-[10vh] z-[-1] translate-x-[-50%] left-[50%] text-transparent bg-clip-text bg-gradient-to-b from-white to-[#000121] mix-blend-exclusion opacity-50">
        Astratinvest
      </p>
    </div>
  );
};

export default PreFooter;
